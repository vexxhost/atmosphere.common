# Copyright (c) 2025 VEXXHOST, Inc.
# SPDX-License-Identifier: Apache-2.0

- name: Deploy "vault-secrets-webhook"
  hosts: "{{ target | default('all') }}"
  gather_facts: false
  roles:
    - role: atmosphere.common.vault_secrets_webhook
      tags:
        - vault-secrets-webhook
