# Copyright (c) 2025 VEXXHOST, Inc.
# SPDX-License-Identifier: Apache-2.0

- name: Get current HelmRelease
  run_once: true
  kubernetes.core.k8s_info:
    api_version: helm.toolkit.fluxcd.io/v2
    kind: HelmRelease
    name: "{{ dellhw_exporter_helm_release_name }}"
    namespace: "{{ dellhw_exporter_helm_release_namespace }}"
  register: dellhw_exporter_helm_release_current

- name: Install "dellhw_exporter"
  run_once: true
  kubernetes.core.k8s:
    server_side_apply:
      field_manager: "atmosphere.common"
      force_conflicts: true
    definition:
      - apiVersion: v1
        kind: Namespace
        metadata:
          name: "{{ dellhw_exporter_helm_release_namespace }}"

      - apiVersion: source.toolkit.fluxcd.io/v1
        kind: HelmRepository
        metadata:
          name: "{{ dellhw_exporter_helm_repository_name }}"
          namespace: "{{ dellhw_exporter_helm_release_namespace }}"
        spec:
          interval: 60m
          url: "{{ dellhw_exporter_helm_repository_url }}"

      - apiVersion: helm.toolkit.fluxcd.io/v2
        kind: HelmRelease
        metadata:
          name: "{{ dellhw_exporter_helm_release_name }}"
          namespace: "{{ dellhw_exporter_helm_release_namespace }}"
        spec:
          interval: 10m
          releaseName: "{{ dellhw_exporter_helm_release_name }}"
          chart:
            spec:
              chart: "{{ dellhw_exporter_helm_chart_name }}"
              version: "{{ dellhw_exporter_helm_chart_version }}"
              sourceRef:
                kind: HelmRepository
                name: "{{ dellhw_exporter_helm_repository_name }}"
          values: >-
            {{
              dellhw_exporter_helm_release_current.resources[0]['spec']['values'] | default({})
              | atmosphere.common.nullify_missing_values(dellhw_exporter_helm_values)
              | atmosphere.common.merge_replace_nulls(_dellhw_exporter_helm_values)
            }}
